import { Action } from "modules/common/reducers/Action";

export const COMPLETE_COMMAND_ACTION_TYPE = 'COMPLETE_COMMAND';

export const completeCommandAction: Action = {
  type: COMPLETE_COMMAND_ACTION_TYPE,
}